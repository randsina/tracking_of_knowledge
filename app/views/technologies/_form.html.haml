= simple_form_for @technology, :html => { :class => 'form-horizontal' } do |f|
  = f.input :name
  = error_span(@technology[:name])
  = f.button :submit, :class => 'btn-primary'
  = link_to t('.cancel', :default => t("helpers.links.cancel")), technologies_path, :class => 'btn btn-default'
